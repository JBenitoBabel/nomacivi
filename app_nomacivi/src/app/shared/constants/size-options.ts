export enum SizeOptions {
    small = 'small',
    normal = 'normal',
    medium = 'medium',
    big = 'big',
    extra = 'extra',
};

export type Size = SizeOptions.small | SizeOptions.normal | SizeOptions.medium | SizeOptions.big | SizeOptions.extra;